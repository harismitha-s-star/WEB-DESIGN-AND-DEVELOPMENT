Consider three variables $a, $b, $c, apply var_dump() function to evaluate if the values
are equal or not.

CODE:
<?php
$a = 10;
$b = "10";
$c = 20;

// Check if values are equal (==)
echo "Using == (value comparison):<br>";
var_dump($a == $b); // true because values are the same
var_dump($a == $c); // false because values differ

echo "<br><br>";

// Check if values and types are equal (===)
echo "Using === (value & type comparison):<br>";
var_dump($a === $b); // false because types differ (int vs string)
var_dump($a === 10); // true because both value & type match
?>

OUTPUT:
Using == (value comparison):
bool(true)
bool(false)
Using === (value & type comparison):
bool(false)
bool(true)
